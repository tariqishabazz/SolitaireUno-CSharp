namespace SolitaireUno.UITests
{
    /*
    [Parallelizable(ParallelScope.Self)]
    
    [TestFixture]
    public class GameBoardTests : PageTest
    {
        [Test]
        public async Task Homepage_ShouldLoad_Successfully()
        {
            // Playwright needs your Blazor app to be running to test it!
            // Make sure to swap the localhost port with the one your app actually uses.
            await Page.GotoAsync("https://localhost:7295/");

            // This checks if the browser tab title contains "SolitaireUno"
            await Expect(Page).ToHaveTitleAsync(new System.Text.RegularExpressions.Regex("SolitaireUno"));
        }
    }
    */
}